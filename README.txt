Pour lancer le projet, il suffit de créer un dossier build et de lancer dans celui-ci la commande suivante:
cmake .. && make && ./src/main

Remarque: pour que cela marche, il faut installer la bibliothèque GSL
